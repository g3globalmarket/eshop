services:
  api-gateway:
    healthcheck:
      test: ["CMD","node","-e","require(\"http\").get(\"http://localhost:8080/gateway-health\", r=>process.exit(r.statusCode===200?0:1)).on(\"error\",()=>process.exit(1))"]
      interval: 30s
      timeout: 10s
      retries: 3
